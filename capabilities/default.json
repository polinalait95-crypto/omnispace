{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Базовые разрешения окна. Своя логика работает через invoke-команды.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
